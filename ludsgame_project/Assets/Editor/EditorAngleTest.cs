using UnityEditor;
using UnityEngine;
using System.Collections;

public class EditorAngleTest : EditorWindow{

	//[MenuItem("AngleTest/Test")]

	static void Init() {
		//EditorWindow window = GetWindow(typeof(EditorAngleTest));
		//window.Show();
	}

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
